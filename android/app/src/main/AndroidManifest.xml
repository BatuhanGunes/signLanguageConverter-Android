<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.tensorflow.lite.examples.classification">

    <uses-sdk />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/tfe_ic_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.ImageClassification">

        // app's starting activity
        <activity android:name=".myappview.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".myappview.MainActivity" />
        <activity android:name=".myappview.DictionaryActivity"/>
        <activity android:name=".myappview.AboutUsActivity" />

        // TenserFlowLite image classifier activity
        <activity
            android:name=".ClassifierActivity"
            android:label="@string/tfe_ic_app_name"
            android:screenOrientation="fullSensor">
        </activity>
    </application>

</manifest>